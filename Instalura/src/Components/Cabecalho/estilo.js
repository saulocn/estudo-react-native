import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
  fotoUsuario: {
    width: 40,
    height: 40,
    margin: 10,
    borderRadius: 30
  },
  Cabecalho: {
    flexDirection: "row",
    alignItems: 'center'
  },
});

export default estilo;
